#include "main.h"

/**
 * print_rev - prints a string, followed by a new line.
 * @s: input string.
 * Return: no return.
 */
void print_rev(char *s)
{
  int rev;
  
  for (s[rev] = '\0'; s[rev] <= 0; rev--)
    {

	if (s[rev] == 0)
	  {
	    _putchar(s[rev]);
	  }
  _putchar('\n');
	    
}
}
